# AI Recipe Optimizer API

Build a FastAPI service that takes ingredients as input and uses a local LLM to generate optimized recipes, then exposes multiple endpoints for different dietary preferences. This demonstrates real API development skills while entering the hot AI market without requiring expensive GPU resources.

## How to run
```
python main.py
```
